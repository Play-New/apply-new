node_modules/
*.bundle.json
digest.json
scratch-*.mjs
.DS_Store

# Everything the tool generates lands in out/ — one folder, one rule.
/out/
# Legacy locations: older versions wrote the working files to the repo root.
/candidate.json
/profile.md
/narrative*.json

# Local Claude Code state (per-candidate); the slash command in
# .claude/commands/apply-new.md stays tracked.
.claude/settings.local.json
.claude/projects/
